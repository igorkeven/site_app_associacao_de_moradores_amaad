import 'package:flutter/material.dart';

class Avisos extends StatelessWidget {
  const Avisos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Avisos Importantes da AMAAD'),
        centerTitle: true,
       
      ),
      body: Container(),
    );
  }
}